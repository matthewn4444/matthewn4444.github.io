diff --git a/Makefile b/Makefile
index 01d7466..a3a4ed2 100644
--- a/Makefile
+++ b/Makefile
@@ -198,7 +198,7 @@ src/subtitles-octopus-worker.bc: dist/libraries/lib/libass.so src/Makefile
 EMCC_COMMON_ARGS = \
 	-s TOTAL_MEMORY=134217728 \
 	-O3 \
-	-s EXPORTED_FUNCTIONS="['_main', '_malloc', '_libassjs_init', '_libassjs_quit', '_libassjs_resize', '_libassjs_render', '_libassjs_free_track', '_libassjs_create_track']" \
+	-s EXPORTED_FUNCTIONS="['_main', '_malloc', '_libassjs_init', '_libassjs_quit', '_libassjs_resize', '_libassjs_render', '_libassjs_free_track', '_libassjs_create_track', '_libassjs_process_data']" \
 	-s EXTRA_EXPORTED_RUNTIME_METHODS="['ccall', 'cwrap', 'getValue', 'FS_createPreloadedFile', 'FS_createFolder']" \
 	-s NO_EXIT_RUNTIME=1 \
 	--use-preload-plugins \
diff --git a/src/post-worker.js b/src/post-worker.js
index 5b1fcca..c46b8b3 100644
--- a/src/post-worker.js
+++ b/src/post-worker.js
@@ -86,6 +86,14 @@ self.setTrackByUrl = function (url) {
     self.setTrack(Module["read"](url));
 };
 
+/**
+ * Adds new dialog event for subtitle track.
+ * @param {!string} content formatted dialog line.
+ */
+self.addDialogEvent = function (content) {
+    self._process_data(content);
+}
+
 self.resize = function (width, height) {
     self.width = width;
     self.height = height;
@@ -748,6 +756,9 @@ function onMessageFromMainEmscriptenThread(message) {
         case 'set-track-by-url':
             self.setTrackByUrl(message.data.url);
             break;
+        case 'add-dialog-event':
+            self.addDialogEvent(message.data.content);
+            break;
         case 'runBenchmark': {
             self.runBenchmark();
             break;
diff --git a/src/pre-worker.js b/src/pre-worker.js
index 3420777..6863c70 100644
--- a/src/pre-worker.js
+++ b/src/pre-worker.js
@@ -50,6 +50,7 @@ Module['onRuntimeInitialized'] = function () {
     self._render = Module['cwrap']('libassjs_render', null, ['number', 'number']);
     self._free_track = Module['cwrap']('libassjs_free_track', null, null);
     self._create_track = Module['cwrap']('libassjs_create_track', null, ['string']);
+    self._process_data = Module['cwrap']('libassjs_process_data', null, ['string']);
     self.quit = Module['cwrap']('libassjs_quit', null, []);
     self.changed = Module._malloc(4);
 
diff --git a/src/subtitles-octopus-worker.c b/src/subtitles-octopus-worker.c
index 1e7e5dd..c8754b4 100644
--- a/src/subtitles-octopus-worker.c
+++ b/src/subtitles-octopus-worker.c
@@ -74,6 +74,10 @@ void libassjs_quit()
     ass_library_done(ass_library);
 }
 
+void libassjs_process_data(char *data) {
+    ass_process_data(track, data, strlen(data));
+}
+
 ASS_Image * libassjs_render(double tm, int *changed)
 {
     ASS_Image *img = ass_render_frame(ass_renderer, track, (int) (tm * 1000), changed);
diff --git a/src/subtitles-octopus.js b/src/subtitles-octopus.js
index d5786e2..26a37dd 100644
--- a/src/subtitles-octopus.js
+++ b/src/subtitles-octopus.js
@@ -449,6 +449,12 @@ var SubtitlesOctopus = function (options) {
         });
     };
 
+    self.addDialogEvent = function (content) {
+        self.worker.postMessage({
+            target: 'add-dialog-event',
+            content: content
+        });
+    };
 
     self.render = self.setCurrentTime;
 
